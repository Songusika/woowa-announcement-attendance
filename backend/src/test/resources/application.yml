spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: create
  flyway:
    enabled: true
logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

common:
  password: 1234

slack:
  bot:
    token: 슬랙 API 봇 토큰
