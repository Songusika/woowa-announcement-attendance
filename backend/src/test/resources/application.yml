spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: create
  flyway:
    enabled: false
logging:
  level:
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

common:
  password: 민트짱1234